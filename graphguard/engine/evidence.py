"""Build evidence tags from graph facts."""

from __future__ import annotations


def build_evidence(facts, tier: str, rule_id: str, reason: str, cli_calls: list) -> list:
    """Build evidence tags (graph_fact, derived, historical).

    Implemented in Phase 1 Step 4.
    """
    raise NotImplementedError("build_evidence — Phase 1 Step 4")
